<!--
  Parent Rule for Windows BITS Client Events

  Provides base rule for Background Intelligent Transfer Service (BITS) events
  from the Microsoft-Windows-Bits-Client/Operational channel.

  Required:
  - Channel must be enabled on Windows:
    wevtutil sl Microsoft-Windows-Bits-Client/Operational /e:true

  Common Event IDs:
  - 3: BITS transfer created
  - 4: BITS transfer completed
  - 59: BITS transfer started
  - 60: BITS transfer stopped
  - 61: BITS transfer cancelled
  - 64: BITS job cancelled by user
  - 65: BITS job cancelled

  Use Cases:
  - Detect malware using BITS for persistence
  - Monitor suspicious file downloads
  - Track data exfiltration via BITS

  Rule ID: 109996 (Custom parent rule for BITS Client)
-->

<group name="windows,bits,">

  <!-- Parent rule for all BITS Client events -->
  <rule id="109996" level="3">
    <field name="win.system.channel">Microsoft-Windows-Bits-Client/Operational</field>
    <description>Windows BITS Client Event</description>
    <options>no_full_log</options>
    <group>windows,bits-client,</group>
  </rule>

</group>
